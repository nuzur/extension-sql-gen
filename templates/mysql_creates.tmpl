{{range $entity := .Entities}}
CREATE TABLE IF NOT EXISTS `{{$entity.Name}}` ( {{range $field := $entity.Fields}}
  `{{$field.Name}}` {{$field.Type}} {{$field.Null}} {{$field.Unique}} {{$field.Default}},{{end}}  
  PRIMARY KEY (`{{$entity.PrimaryKeysIdentifiers}}`),{{range $index := $entity.Indexes}}
  INDEX `{{$index.Name}}` {{$index.FieldNamesIdentifiers}}{{if eq $index.HasComma true }},{{end}}{{end}})  
ENGINE = InnoDB;
{{end}}